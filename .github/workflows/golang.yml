name: Backend CI

on:
  push:

jobs:
  ci:
    runs-on: ubuntu-latest
    env:
      DOCKER_BUILDKIT: "1"
    steps:
      - uses: actions/checkout@v3

      - uses: Jerome1337/gofmt-action@v1.0.4

      - name: Run lint
        run: make lint

      - name: Start DB
        run: make db

      - name: Run unit tests
        run: make unit-test
